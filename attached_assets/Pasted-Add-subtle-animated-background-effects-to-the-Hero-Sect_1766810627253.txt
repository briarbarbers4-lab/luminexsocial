Add subtle animated background effects to the Hero Section (currently plain black):

=== BACKGROUND DESIGN ===

Keep base background: #0B0D12 (Primary Dark)

ADD flowing animated lines/gradient mesh:

OPTION 1 - Flowing Curved Lines (Preferred):
- Add 3-4 curved SVG lines flowing across background
- Lines color: rgba(13, 33, 161, 0.2) (Royal Blue with low opacity)
- Lines should be smooth curves (bezier paths)
- Position: diagonal across screen, flowing naturally
- Width: 2-3px, with blur effect
- Animate: slow movement (10-15s duration, infinite loop)
- Subtle glow effect on lines

OPTION 2 - Gradient Mesh Orbs:
- Add 2-3 large radial gradients in background
- Colors: 
  * rgba(13, 33, 161, 0.15) (Royal Blue)
  * rgba(140, 143, 148, 0.1) (Deep Purple)
- Position: top-right, bottom-left, center
- Size: 400-600px diameter
- Blur: heavy (80-100px)
- Animate: slow floating movement

OPTION 3 - Grid Pattern (Subtle):
- Add faint grid lines in background
- Color: rgba(247, 248, 252, 0.03)
- Grid size: 50px x 50px
- Fade out toward edges (vignette effect)
- Optional: add perspective for depth

=== IMPLEMENTATION ===

CSS Example for Gradient Mesh:
```css
.hero-section {
  position: relative;
  background: #0B0D12;
  overflow: hidden;
}

.hero-section::before {
  content: '';
  position: absolute;
  top: -10%;
  right: -10%;
  width: 600px;
  height: 600px;
  background: radial-gradient(circle, rgba(13, 33, 161, 0.15) 0%, transparent 70%);
  filter: blur(80px);
  animation: float 12s ease-in-out infinite;
}

.hero-section::after {
  content: '';
  position: absolute;
  bottom: -10%;
  left: -10%;
  width: 500px;
  height: 500px;
  background: radial-gradient(circle, rgba(140, 143, 148, 0.1) 0%, transparent 70%);
  filter: blur(80px);
  animation: float 15s ease-in-out infinite reverse;
}

@keyframes float {
  0%, 100% { transform: translate(0, 0); }
  50% { transform: translate(30px, -30px); }
}
```

For SVG Curved Lines:
- Create smooth bezier curve paths
- Animate along path or with transform
- Keep subtle and non-distracting
- Layer behind all content (z-index: 1)

=== REQUIREMENTS ===

- Background effects must be SUBTLE (not distracting)
- Don't interfere with text readability
- Animate smoothly (60fps)
- Layer properly: background effects → content → buttons
- Mobile: reduce complexity or simplify effects
- Keep dark theme consistent

Add depth and visual interest while maintaining professional look.